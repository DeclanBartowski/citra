/******/ (function() { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
document.addEventListener('DOMContentLoaded', function () {
  var swiperCarAuto = new Swiper('.swiper-car-auto', {
    // Optional parameters
    loop: false,
    slidesPerView: "auto",
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev'
    }
  });
  var swiperCar = new Swiper('.swiper-car', {
    // Optional parameters
    loop: true,
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev'
    }
  });
  Fancybox.bind('[data-fancybox="certificates"]', {
    Thumbs: false,
    Toolbar: false,
    Image: {
      zoom: false,
      click: false,
      wheel: "slide"
    }
  });
  window.addEventListener('scroll', function () {
    var scrollDistance = window.scrollY;

    if (window.innerWidth > 768) {
      document.querySelectorAll('.scroll-section').forEach(function (el, i) {
        if (el.offsetTop - document.querySelector('.js-scroll-list').clientHeight <= scrollDistance) {
          document.querySelectorAll('.js-scroll-list li').forEach(function (el) {
            if (el.classList.contains('active')) {
              el.classList.remove('active');
            }
          });
          document.querySelectorAll('.js-scroll-list li')[i].classList.add('active');
        }
      });
    }
  });
  document.querySelectorAll('.main-menu__list li a').forEach(function (el, i) {
    el.addEventListener('click', function (e) {
      var header = document.querySelector('.header');
      header.classList.remove('active');
    });
  });
  $('.menu-btn').click(function () {
    $('.header').toggleClass('active');
  });
  $(document).ready(function () {
    $('.input').val('');
  });
  $('body').on('input', '.input', function () {
    if (!this.value.length == 0) {
      this.classList.add('active');
    }

    ;
  });
});
/******/ })()
;